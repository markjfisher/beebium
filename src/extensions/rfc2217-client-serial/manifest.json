{
    "name": "rfc2217-client-serial",
    "display_name": "RFC 2217 Serial (client)",
    "description": "Connect the BBC serial port (RS423) to a remote RFC 2217 access server over TCP",
    "library": "rfc2217-client-serial",
    "cli": "rfc2217-client-serial",
    "parameters": [
        {
            "key": "url",
            "type": "string",
            "description": "Access-server endpoint as [scheme://]host:port (e.g. url=\"rfc2217://ser2net.local:4001\"); alternative to host/port, cannot be combined with them"
        },
        {
            "key": "host",
            "type": "string",
            "description": "RFC 2217 access-server hostname or address (alternative to url)"
        },
        {
            "key": "port",
            "type": "integer",
            "description": "RFC 2217 access-server TCP port (alternative to url)"
        },
        {
            "key": "baud",
            "type": "integer",
            "default": "19200",
            "description": "baud rate to set on the remote UART (real hardware)"
        },
        {
            "key": "framing",
            "type": "string",
            "default": "8N1",
            "description": "remote UART framing as <data><parity><stop> (data 5-8; parity N/O/E/M/S; stop 1/2), e.g. 8N1 or 7E1"
        },
        {
            "key": "flow",
            "type": "string",
            "default": "none",
            "description": "remote UART flow control: none, rtscts (hardware) or xonxoff"
        },
        {
            "key": "dtr",
            "type": "boolean",
            "default": "true",
            "description": "assert DTR on the remote UART (many devices/modems need it; the BBC connector has no DTR pin)"
        },
        {
            "key": "tx_buffer",
            "type": "integer",
            "default": "4096",
            "description": "transmit buffer size in bytes; the BBC's /CTS asserts at/above it"
        }
    ]
}
