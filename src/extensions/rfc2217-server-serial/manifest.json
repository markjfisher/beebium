{
    "name": "rfc2217-server-serial",
    "display_name": "RFC 2217 Serial (server)",
    "description": "Expose the emulated BBC serial port (RS423) to RFC 2217 clients over TCP",
    "library": "rfc2217-server-serial",
    "cli": "rfc2217-server-serial",
    "parameters": [
        {
            "key": "bind",
            "type": "string",
            "default": "127.0.0.1",
            "description": "listen address; the service is UNAUTHENTICATED plain Telnet, so bind to a wider address only behind SSH/WireGuard"
        },
        {
            "key": "port",
            "type": "integer",
            "required": true,
            "description": "TCP port to listen on"
        },
        {
            "key": "tx_buffer",
            "type": "integer",
            "default": "4096",
            "description": "transmit buffer size in bytes; the BBC's /CTS asserts at/above it"
        }
    ]
}
