[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "beebium-tube-save-tests"
version = "0.1.0"
description = "Tube DFS SAVE integration tests for the Beebium BBC Micro emulator"
license = "GPL-3.0-or-later"
requires-python = ">=3.12"
authors = [
    { name = "Robert Smallshire", email = "robert@smallshire.org.uk" }
]
dependencies = [
    "beebium",
    "beebium-adfs-tests",
    "oaknut-dfs",
    "pytest>=7.0.0",
    "pytest-timeout>=2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/tube_save_support"]

[tool.uv.sources]
beebium = { path = "../../clients/python", editable = true }
beebium-adfs-tests = { path = "../adfs", editable = true }

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
