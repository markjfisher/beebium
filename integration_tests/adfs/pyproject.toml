[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "beebium-adfs-tests"
version = "0.1.0"
description = "ADFS integration tests for the Beebium BBC Micro emulator"
license = "GPL-3.0-or-later"
requires-python = ">=3.12"
authors = [
    { name = "Robert Smallshire", email = "robert@smallshire.org.uk" }
]
dependencies = [
    "adfs-disc-tools",
    "beebium",
    "oaknut-dfs",
    "pytest>=7.0.0",
    "pytest-timeout>=2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/adfs_test_support"]

[tool.uv.sources]
adfs-disc-tools = { path = "../../scripts/adfs-disc-tools", editable = true }
beebium = { path = "../../clients/python", editable = true }

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
